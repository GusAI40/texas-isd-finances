# Runtime-only requirements used for the Vercel serverless deployment
# (swapped in as requirements.txt in the deploy payload). Full API including
# NLP; excludes the data-prep/visualization libraries (pandas, matplotlib,
# seaborn, plotly, openpyxl) that only offline scripts use, keeping the
# bundle far under Vercel's 500 MB uncompressed cap.
python-dotenv>=1.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0
langchain>=1.0.0,<2.0.0
langchain-community>=0.4.0
langchain-openai>=1.0.0
openai>=2.0.0
fastapi>=0.115.0
pydantic>=2.7.0
