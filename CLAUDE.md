# Texas ISD Finances — Agent Boot File

You have no memory of previous sessions. This file plus
`docs/ENGINEERING_LOG.md` IS your memory. **Run `/ariba` to catch up before
substantial work, and `/ariba save` before ending any session that changed
anything.** (Skill: `.claude/skills/ariba/SKILL.md`.)

For the evidence-backed runtime, services, data sources, and official Agent
Skills adoption map, read `docs/TECH_STACK_AND_AGENT_SKILLS.md`. External
skills are guidance, not project authority, and are not installed by that
document.

## What this project is

Public transparency portal for Texas school district finances (TEA PEIMS
data, fiscal 2009–2025). FastAPI serves a static portal + JSON API;
LangChain 1.x + DeepSeek (with OpenAI as a configuration-selected alternate)
turns plain-English questions into SQL against two read-only Postgres views on
Supabase. MIT-licensed, built for public use.

## Production (live)

| Thing | Value |
|---|---|
| ⚠️ vercel.json | **Must have NO `rewrites` block.** Vercel changed backend-framework routing so an internal rewrite now passes the DESTINATION path to the app — the old `/(.*) → /api/index` rewrite made FastAPI receive `/api/index` for every request and 404 the entire site while the build still reported READY. The `fastapi` framework preset routes to `api/index.py` on its own. |
| Vercel team | **TAG-ai** (`tag-ai-projects`) — NOT GOAT-UIX. Two teams each hold a project named `texas-isd-finances`; only the TAG-ai one owns `txisd.dev`. Always `--scope tag-ai-projects`. `vercel link` rewrites `vercel.json` and the project framework — `cat vercel.json` after. |
| Live portal | **https://txisd.dev** — the custom domain, use it everywhere. `texas-isd-finances.vercel.app` also resolves but is the prototype URL and must not appear in pages, docs or citations (a test enforces this). Vercel project `texas-isd-finances`, account `tag-ai`. |
| API docs | https://txisd.dev/docs |
| Health check | `GET /health` → require `{"status":"healthy","database":"connected","tracking_schema":"ready"}` plus the expected `revision` after deploy |
| Database | Supabase project `texas-isd-finances`, ref `zwhvabkvrexphlskubog`, us-east-1, org "GOAT-UIX" |
| Data loaded | 20,587 rows · 1,310 districts · fiscal 2009–2025 (TEA "Summarized PEIMS Actual Financial Data") |
| Secrets | ONLY in Vercel env vars (`SUPABASE_DB_URL`, `NLP_DB_URL`, selected provider key, `CRON_SECRET`, `SITE_PASSWORD`, and armed outreach values). Never in repo, never in this file, never in the log. Presence must be verified independently; documentation is not proof a secret exists. |
| LLM provider | One config: `src/llm_config.py`. `DEEPSEEK_API_KEY` present → DeepSeek (`deepseek-v4-flash` @ `https://api.deepseek.com`); else OpenAI. DeepSeek is OpenAI-protocol compatible (incl. tool calls), so `ChatOpenAI` serves both — only base URL + model change. `/health` reports the live provider. |
| Site lock | `SITE_PASSWORD` set → whole site behind a browser password. `/health` and `/api/cron/*` stay open by design. Unset it to go public. |
| GitHub | https://github.com/GusAI40/texas-isd-finances — default branch `master`; inspect Git for the current work branch and exact revision rather than relying on a recorded branch name |

## Invariants (violate these and production breaks)

- **DB connections from deploys must use the Supabase pooler**, not the
  direct host (direct is IPv6-only). Serverless (Vercel) uses transaction
  mode, port **6543**; the API sets asyncpg `statement_cache_size=0` to
  survive it — do not remove that.
- **LangChain is pinned `>=1.0,<2.0`**; the agent API is
  `langchain.agents.create_agent`. The sunset `langchain-community` SQL
  toolkit is gone: `src/nlp_engine.py` owns the four documented SQL tools,
  parses exactly one PostgreSQL query through a reviewed SQLGlot node/function
  allowlist, resolves only two finance views, rejects recursive CTEs, qualifies
  them as `public.*`, sets `search_path` to `pg_catalog`, caps model-visible
  rows, and reasserts timeout-bounded read-only transactions. `NLP_DB_URL` is
  required; never restore the owner fallback. “SELECT-only” is not enough.
- **`data/district_crosswalk.csv` is the district registry** (1,310 rows, committed, whitelisted in `.gitignore`). One row per district: TEA number, county + code, `brb_id`, charter flag, boundary flag, first/last year, **former names** (103) and **aliases** (37 — `Edgewood ISDa`, `Calhoun Co ISD`, `Aransas County ISD`). Built by `scripts/build_district_crosswalk.py`, and **load-bearing**: `Resolver.from_crosswalk()` registers current name + former names + aliases, so every name a district has ever carried resolves. Building the resolver from the finance file alone learned only the EARLIEST name — **64 of the 103 renamed districts could not be resolved by the name they go by today** ("Aransas County ISD" worked, "Rockport-Fulton ISD" did not), so any source using a current name was silently dropped. Now 0. It also turned the bond layer's 13 heuristic `prefix+county` matches into exact ones. Setup order: build the bond file first, then the crosswalk (the builder falls back to `from_tea` if the crosswalk is absent). **A UUID was considered and rejected**: the TEA number already survives renames (103 did, none was ever reused), it is the only id a reader can check against the state, and its first three digits ARE the county code — which is what fixed the bond join. A minted id would add a third thing to sync, would not touch the hard step (sources send NAMES), and is verifiable against nobody but us. `tests/test_crosswalk.py` fails the build if any column looks minted.
- **`district_number` is a 6-digit string** (e.g. `057905` = Dallas ISD).
  Any pandas read of the CSV needs `dtype={"district_number": str}` or
  leading zeros die.
- **Setup order is import-first**: `scripts/prepare_data.py` →
  `scripts/import_to_supabase.py` (creates the 140-col table) → run
  `sql/create_tables.sql` (adds PK/indexes/views/matview/RLS/grants).
- **All Python build paths use the committed universal `uv.lock`.** Vercel
  installs only the base `[project]` dependencies (no pandas/matplotlib —
  bundle cap 500 MB); CI selects all extras + dev; Docker and Render select
  the `server` extra. Pre-flight with `uv lock --check`; a dependency change
  without a regenerated lock must fail.
- **Commits must be authored by a Vercel team seat holder.** Vercel Pro
  bills per seat and returns `BLOCKED` /`TEAM_ACCESS_REQUIRED` for deploys
  whose git author has no seat — it looks like a build failure and isn't.
  Keep `git config user.email` on the repo owner; `Co-Authored-By` records
  the AI author.
- **After any data import**: `REFRESH MATERIALIZED VIEW v_anomaly_flags;`
- **The NLP prompt must keep the "counting is not limiting" rule.** It once
  said "default LIMIT 100", so the agent LIMITed a `SELECT DISTINCT` and
  answered "100 districts" instead of 1,310 — intermittently. Never let a
  default LIMIT be described without excluding aggregates.
- **`/districts?limit=N` returns the first N ALPHABETICALLY**, not a sample.
  Never resolve a district by scanning it; use the district's own endpoints.
- **`static/district_geo.json` is a committed build artifact too.** Rebuild
  order after a TEA release: `build_outcomes_data.py` → `build_district_geo.py`
  (the latter merges the former's measures in).
- **`static/outcomes_data.json` is a committed build artifact** — the API
  serves it directly and it works with no database. Rebuild it after any new
  TEA release: `scripts/ingest_tea_snapshot.py --download` then
  `scripts/build_outcomes_data.py`. `.vercelignore` must never exclude
  `static/`.
- Local sandbox note: direct Postgres (port 5432/6543) may be blocked in
  the remote dev container — use the Supabase Management API
  (`POST /v1/projects/{ref}/database/query`) for SQL and PostgREST for bulk
  data, both over HTTPS.

## Verify before claiming anything works

```bash
ruff check . && python -m pytest -q       # all must pass (count drifts; don't hardcode it)
curl -s https://txisd.dev/health
python scripts/verify_live.py             # does PRODUCTION serve what this tree says?
```

**Run `/code-review` BEFORE the merge, not after.** Two review passes on
2026-08-15/16 found **fifteen** real defects in work already reported as done
and verified, with a green suite behind every one of them. Two were
user-facing (the map credited TIGER 2024 while serving 2025; the private ops
route answered 500 instead of 404 for a non-ASCII token, confirming it exists),
and one was a **test that validated itself** — the twin-boundary check compared
`district_geo.json` against `has_boundary`, which is generated FROM it, so
reintroducing the bug and rebuilding both left the suite green while five
districts sat on the wrong land. A passing suite is evidence about the tests,
not about the code. The review is cheap; shipping and then finding out is not.

**`verify_live.py` is the check the other three could not make.**
`verify_sources.py` proves the publisher's file is the publisher's file,
`test_provenance.py` re-derives every headline from it, `verify_artifacts.py`
rebuilds and byte-diffs — and a repo can pass all three while the deployed site
serves something else entirely. That is not hypothetical: the bond layer ran
live for weeks two years stale, publicly naming districts as having no
voter-approved bond after they had passed one, with a green suite throughout.
It exits non-zero on drift, so it works as a deploy gate or a cron. Run it
against a local server (`--base http://127.0.0.1:8000`) to confirm a build
before shipping it.

**Static pages need their own check — a 200 response proves nothing.** A
`const` redeclaration once killed `static/map.html` entirely while the page
still served 200 and grepped fine. Always parse the script, and render it:

```bash
python3 -c "import re;s=open('static/map.html').read();\
open('/tmp/m.js','w').write(re.findall(r'<script>(.*?)</script>',s,re.S)[-1])"
node --check /tmp/m.js                    # repeat for static/index.html
```

Never verify a UI change by grepping the served HTML for strings.

**Anything above the fold that depends on `localStorage` must be applied
before first paint** — inline `<script>` in `<head>` setting a class on
`documentElement`, plus CSS. Doing it in `boot()` causes a visible flash that
lasts as long as whatever `boot()` awaits first (this shipped once).

To render *production* in a browser (Chromium can't TLS through the agent
proxy), run `scratchpad/liveproxy.py` and point Playwright at
`127.0.0.1:8799`.

## Current Status (updated 2026-08-25 — keep this a snapshot, history goes in the log)

The newest bullets control. Dated bullets below retain useful engineering
history and may contain superseded counts; never let them override the current
outreach item or `data/outreach_watermark.json`.

- 🟡 **Forensic hardening is repository-complete; production requires separate
  proof** (2026-08-22) — the change set explicitly monitors both Vercel crons,
  surfaces missing reply/KPI configuration as `NOT CONFIGURED`, pins build and
  Action inputs, locks Python resolution, and removes the SQL agent's sunset
  toolkit and database-owner fallback. Repository state does not prove merge,
  deployment, or live readiness: check Git/CI/Vercel and run post-deploy
  verification. At the audit-time live check, `outreach-drain` fired but
  reported `unarmed`; delivery remains externally blocked until the required
  outreach values are rotated/configured in the TAG-ai Vercel project and the
  live cron reports `empty` or `ok`. Never place those values in chat or Git.
- ✅ **Server-side outreach and durable privacy fixes are on `master`** (PRs #48 and #51) — the deployed code includes durable opt-outs, fail-closed send-time suppression, aggregate-only public reply logs, `outreach_contact` + `outreach_queue`, shared rendering, and hidden enqueue/drain/status endpoints. The 2026-08-19 log entry predates those merges and is history, not current merge state. Repository inspection cannot prove the credential-dependent delivery and reply jobs are armed; verify operational readiness without exposing secret values before sending. 671/1,019 historical sends are recorded; do not infer that the remaining queue is ready.
- ✅ **Dashboard honesty (2026-08-19)** — sessions are a WHITELIST over browser-rendered events (`intel.BROWSER_SESSION_EVENTS`; the pixel, the reply ingest and cookieless scanner clicks all fabricate session ids); funnel "engaged" = 3+ sections on screen (2 of 100, not 12 — a 4-district appliance cluster and parked tabs were counting as readers). `ensure_schema` now re-applies a stale `v_recipient_journey` (pg_get_viewdef marker `'followup'`) — a sentinel answers "does it exist", never "is it current". `/ops/outreach-data` had 500'd in prod since 08-14 (`.vercelignore` dropped the crosswalk); `tests/test_vercelignore.py` now evaluates the rules with real gitignore semantics against everything `src/` reads.
- ✅ **National layer LIVE (2026-08-17, PR #25, verify_live 31/31)** — `/national/texas` + `/district/{n}/national` + the "Against the whole country" econ card, from `static/national_data.json` (no DB). Census F-33 FY2024 + NPEFS state file + CCD LEA Directory bridge. **Texas 44th of 51** (50 states + DC) at $13,642/student in ADA; every district gets Census's own per-pupil current spending + percentile among the 9,294 U.S. districts with 500+ students (floor, never round). ⚠️ **The TEA↔NCES join is the CCD directory's `ST_LEAID`, keyed by number** — NCES ids are NOT derivable from TEA numbers (Dallas = 4816230). ⚠️ **PPCSTOT is published, never recomputed**: the Census excludes spending for students outside fall membership, so `TCURSPND/ENROLL` is ~1% wrong; the artifact quotes the Census definition verbatim. ⚠️ Charters (open and closed) have NO F-33 row — the Census surveys governments — and get `not_applicable`, never "missing information". Fiscal 2024, one year behind TEA; never mixed in one figure. Raw xlsx/CSV in `data/` are gitignored; the 57-row NPEFS file IS committed so the state-rank re-derivation runs in CI (`tests/test_national.py`).
- ✅ **Header fixed after owner report, browser-diagnosed (2026-08-17)** — "More" was a CLIPPED dropdown (`overflow-x:auto` on `.m-tabs` clips absolute children), now a sibling of the tab strip on all 10 pages; same-destination masthead clicks scroll to top instead of silently reloading; **the ?d decoration resolves at CLICK time** so an in-page district switch no longer navigates to the previous district. All test-locked.
- ✅ **District news ticker on `/`** — the Feed's stories under Live Figures, one whole headline at a time via the shared `rotateStrip` (one rotator for both strips, by design). Reads **`/briefing`** (1h shared cache, guard mirrors the caching middleware; server-side fallback to the committed snapshot) — NOT `/static/isd_briefing.json`, which 404s because the site deliberately has no blanket static mount. `.ticker[hidden]{display:none !important}` because an author display beats UA `[hidden]`. Wraps on ≤620px (tag+Feed link on top, headline full-width). `/sources` eyebrow counts are digits, test-pinned to the register. **The brand link is HOME**: never `?d`-decorated, lands on the statewide landing; same-destination masthead clicks scroll to top.
- ✅ **HISD press brief** — `docs/HISD_TAKEOVER_BRIEF.md` + designed shareable artifact (claude.ai, private until shared). `district_national` is MCP tool **eleven**, verified live; its figures are read from the artifact at call time and its absences reuse `src/absences.py` sentences.
- ✅ **E-Rate layer LIVE (2026-08-17, PR #29)** — `/erate/texas` + `/district/{n}/erate` + the "Federal internet money" econ card, from `static/erate_data.json` (no DB). USAC pays discounts to vendors, so this money is in NO PEIMS file. **$2.2B Funded FY2017–2026, closed years drawn 86–94%.** ⚠️ **Funded-only, always**: the dataset carries one row per FORM VERSION — a Pending row's `funding_commitment_request` is the amount ASKED (FY2018 all-status $657M vs Funded $301M — a scandal-shaped category error caught before publishing). Ingest verifies no FRN has two Funded rows; paging orders by the unique (frn,status) composite. ⚠️ **BEN→TEA match refuses disagreement** (`data/erate_district_match.csv`, 1,078/1,118, 97.4% of dollars): state code / unanimous-before-filter campus prefixes (≥2 coded children alone) / name+county with retry-only spelling fixups and charter-flag-gated prefix matches. Strip trailing " County" from USAC counties or twin names go unmatched (Wilbarger Northside shipped a false absence briefly). Charter networks under one BEN and consortium money ($299M) are never attributed to a district. ⚠️ Builder's open-year clock reads the ingest vintage in `freshness_vintages.json`, never `date.today()` (verify_artifacts byte-diff). USAC's rowsUpdatedAt churns constantly — re-ingest on cadence, not per freshness fire.
- ✅ **Beta + feedback LIVE (2026-08-17, PR #45)** — a `Beta` chip in the masthead of all ten pages IS the invitation (not a label beside a link nobody presses); it opens one dialog in `ask.js` (reused, not a second widget) asking what the reader came for. `POST /feedback` → `site_feedback`; notes rank ABOVE every counted threshold in the improvement panel, because telemetry says what people did and a note says what they could not find. ⚠️ **Email is never required and the form says so**; the dashboard shows WHETHER contact was left, never the address. ⚠️ **This caught a live disclosure breach**: /about promised a question is never "linked to another question you asked", which `chat_turn` made false the moment the intelligence layer merged — rewritten, with a test that fails the build if the retired promise returns. ⚠️ The migration sentinel moved to `site_feedback`; leaving it on `chat_turn` would have made the migration a silent no-op on every database that already ran the earlier version.
- ✅ **Partner credit removed from the portal (2026-08-17, PR #46)** — no third-party individual, employer, phone or licence number is named on any public page; replaced by a build-one-for-your-data offer that ends "we will tell you honestly whether it is worth doing". ⚠️ **`COLDWELL EL` in `campus_data.json` is a REAL Texas elementary school** in TEA's accountability file — a blanket word ban deletes a school from the campus layer. The test says so. ⚠️ Still present in the REPO (not the portal): `CLAUDE.md`, `docs/ENGINEERING_LOG.md`, `docs/AUDIT_2026-07-31.md` — engineering history, owner's call whether to rewrite.
- ✅ **Intelligence layer LIVE (2026-08-17, PR #42/#43)** — `/ops/intel` (OPS_TOKEN, 404-not-403): who engaged, with what, what they asked, and one person's whole journey. **Recipients only** — the owner chose to keep the published promise, so anonymous visitors stay a number in `site_visits`. Widened `visitor_event` (section/detail/conversation_id/event_key, 10 event kinds) rather than forking a second analytics table. `chat_turn` holds questions and answers keyed by an opaque conversation id and carries **no identity**; only a `question` event attaches a person. A client may assert ONLY `section`/`download`/`followup` — everything else is server-written, because a client that could post `reply` could manufacture the only conversion. ⚠️ **Two populations, named in the payload**: funnel/people/accounts are recipients; question panels are every visitor. ⚠️ **Regression is REFUSED in code** (`intel.power()`) and prints why — a handful of conversions supports zero predictors. ⚠️ **`client_ip()` must return None for a non-address**: the column is `inet`, so a hostname raised inside a fail-open write and silently discarded the ENTIRE click/pageview/open. Found only by running the real flow against a real Postgres (`initdb` throwaway cluster; the identity cookie is `Secure`, so a TestClient on http never sends it and tracking looks broken). Live first read: 100 tracked, 52 opened, 17 clicked, 10 engaged.
- ✅ **Reply ingest — the funnel finally ends somewhere** (`scripts/ingest_replies.py` + daily `.github/workflows/replies.yml`) — reads the inbox READ-ONLY over stdlib IMAP, matches senders against who we mailed, records the FACT and TIME only (never a subject or body — correspondence, not telemetry). Attributes to the most recent send BEFORE the reply, refuses one that predates every send, idempotent on `reply:{rid}:{message_id}` because a daily cron re-reads the same three weeks. Auto-honours opt-out requests (a false positive costs one email; a false negative breaks a written promise). ⚠️ Needs `IMAP_USER` + a Gmail **APP password** + `SUPABASE_PAT` as repo secrets; manual missing-secret runs are readiness probes, while scheduled missing-secret runs warn, record `NOT CONFIGURED`, and fail. **Verified against the real mailbox 2026-08-17: zero superintendent replies in 14 days — a finding, not a broken pipe.**
- ✅ **The answer contract — LIVE 2026-08-17, PR #41, verify_live 37/37 including `--with-query`** — readers were seeing literal `**bold**` and `| pipes |` in the ask sheet. That looked like a missing Markdown renderer; it was `/query` returning one string of model prose and the sheet printing it with `textContent`. `src/answer.py` is the fix: **trusted data → deterministic calculation → STRUCTURED ANSWER → UI**. `/query` now returns `structured` beside `answer` — `{kind, lead, blocks, figures, comparison, sources, limitations, follow_ups}` — and `static/ask.js` draws components from it.
  ⚠️ **The model's prose becomes DATA, never markup.** Inline text is parsed into `{t, b}` runs, so the renderer has exactly two branches — a text node or a `<b>` — and nothing else can happen. `innerHTML` is still reserved for this file's own constant strings. A `<script>` tag in an answer renders as an inert string.
  ⚠️ **The figures and the ranking are OURS, never parsed out of prose.** Metric cards read `static/economics_data.json` (each carrying the lineage metric that opens its own working, in place, inside the sheet); the ranked comparison is `who_does_better`, matched at build time. **A figure lifted from a sentence is only as right as the sentence.** The `total_per_student` card does not exist — operating + debt is a SUM, and the composed-total rule bans division lineage on it.
  ⚠️ **The cards can legitimately disagree with the answer above them, and now say so.** The model reads the finance VIEW (all funds, construction included: Dallas $23,420); the cards read the economics artefact (operating $14,210 + debt $3,578, no construction). Both are right and answer different questions — the caption states the basis rather than leaving a reader to reconcile two numbers on one screen.
  ⚠️ **One renderer, two places it draws.** `static/index.html` had its own copy of the answer renderer, which is exactly how the landing page kept printing raw Markdown after the sheet had components — two renderers means one of them is always the older one. It now calls `window.TISDAsk.render(box, structured, alive)`; `streamAnswer` survives only as the plain-text fallback.
  ⚠️ Follow-ups are `{label, question}` pairs generated from the question KIND and the district in context — **every one maps to data this repo publishes**, because a suggestion the system cannot answer advertises a capability and then fails. Near-duplicates of what was just asked are dropped. The chip shows the label and asks the full question.
  Verified in a driven browser at 1280×900 and 390×844, light and dark: components draw, tables scroll inside their own box (page overflow 0), the lineage panel returns VERIFIED with its division, follow-up chips open the sheet. `--with-query` in `verify_live.py` now fails if a deployment answers correctly but ships no structured answer.
- ✅ **Lineage wave 2 (2026-08-17)** — the spending dollar and the statewide headline are now clickable evidence, not just figures. The money clock's `$109.4B` opens `GET /lineage/texas/statewide_total_spend` (VERIFIED, arithmetic honestly **n/a** — it is a sum), the penny caption's per-student figure opens the division, and the allocation card's teaching/debt/operating figures open theirs (`allocation.lineage`). Second road `scripts/recompute_spending.py` (stdlib csv, AST-checked independent); build refuses on any of 8,416 disagreements; fixture now freezes `all_funds_total_disbursements` so CI re-derives the headline from the SHA-anchored sums. `district_lineage` MCP metric enum grew the three spend metrics. verify_live 36 checks (was 33).
- ⏭️ **Fountains next**: TAPR campus files (TEA SAS broker CGI form POST, no login — old DownloadData.html is gone; needs a session to map form params), NPEFS history, SAIPE; E-Rate MCP tool if wanted.

- ✅ **Answerability measured, and raised D→B** (2026-08-25) — `scripts/coverage_simulation.py`
  runs a 90-day Monte Carlo over **60,042 inquiries** and resolves each one by probing the real
  artefacts, never by asking a model. **67.1% → 82.2%** across three waves; `financial` moved
  **D→A**, `root_cause` **F→A**, and the "held but unreachable" category is now **empty**.
  `scripts/ai_performance_probe.py` measures the separate question — given data we hold, does the
  agent return the right number (16/20 at last live run). Full report and the honest ceiling:
  `docs/COVERAGE_SIMULATION_2026-08-23.md` §14. **90% is not reachable from public Texas data**
  without publishing an enrollment projection and a current-year budget the state has not released.
- ✅ **Three layers added, none needing a new source** (2026-08-25) — `spending_detail.json`
  (all 16 PEIMS **function** codes — buses, meals, counsellors, administration — plus 8 **program**
  codes incl. special education and 4 **object** codes; the columns were already in
  `texas_school_finance`), `tax_history.json` (per-district rate + taxable value 2009–2024, already
  in `data/tea_property.csv`, only the latest row ever read — **the rate FELL in 930 of 1,015
  districts** while bills rose), and `campus_performance.json` (TAPR: STAAR by subject for 8,264
  campuses, by 15 student groups for 1,204 districts). `v_finance_summary` widened **12 → 30
  columns** so the agent reaches them too. ⚠️ **A share is of OPERATING, never total** — total
  carries bond-funded construction. ⚠️ **An unreported function is omitted, never zero**, or every
  non-reporter ranks as the thriftiest in Texas.
- ⚠️ **TAPR is NOT blocked** — the earlier note saying its download needs a human is wrong.
  `scripts/ingest_tapr.py` walks the SAS broker's stateless three-step wizard; `--list` shows all 33
  datasets. **`var_type` (N/D/R) is the trap**: STAAR datasets require it, staff datasets do not, and
  omitting it returns **HTTP 200 with a well-formed CSV holding only campus names** — 4 columns where
  949 were expected, no error. The ingest refuses ≤6 columns. **TAPR also encodes missing data as
  `-1` and `-3`, not blanks** (115,038 + 8,464 cells in one file); they parse as valid floats and the
  first build published districts scoring "-1%".
- ✅ **The governance loop is closed (PR #67, live)** — `isd_review_queue` had been written on every
  cron firing and **read by nothing**. `/ops/review` + `/ops/review-data` + `/ops/review-decide` now
  work it; first live read found **497 open items** accumulated invisibly. The surface records a
  status and **nothing else** — it cannot publish, send, or edit a briefing — and resolving twice
  returns **409, not an overwrite**, so a disagreement stays visible.
  `public.outreach_outcome` records what actually happened, **entered by a person and never
  inferred**: a test fails the build if the recorder touches `visitor_event`, `outreach_status`,
  dwell, opens or clicks. `/ops/outcomes` publishes `districts_never_followed_up` (**671**) as the
  honest denominator — nine outcomes against 671 mailed is not a conversion rate.
  `public.outreach_run` ties a human's enqueue to the crons that drain it; `authorized_by` stores the
  **name of the gate, never a token**.
- ✅ **`/query` answers are now checked against published figures** (`src/answer_check.py`) — the
  prompt never listed `operating_spend`, so the agent substituted `total_spend` four times out of
  four (Tioga ISD 2014: $5,603,166 against a true $3,205,610). Four verdicts, and `unchecked` is
  deliberately **not** `agrees`. It annotates and never edits. Token telemetry now lands on
  `nlp_usage` (summed across the agent loop, zero rather than an estimate when a provider is silent).
  `answer-quality.yml` holds the live agent to a floor **weekly** — cadence is the only thing
  bounding its cost until a spend cap exists.
- ✅ **`docs/DATA_DICTIONARY.md`** (2026-08-25) — 20 files, 617 fields, 78 routes, **generated** from
  the real files by `scripts/build_data_dictionary.py`; a test fails the build if it drifts, if a new
  artefact lacks a plain-English entry, or if the builder stops reading real files.
- ⚠️ **`verify_artifacts` no longer cries wolf** — a raw source absent from this machine means the
  artefact was **NOT CHECKED**, which is different from checking it and finding it clean. With
  nothing rebuildable it prints `NOTHING WAS VERIFIED` rather than "every committed artefact matches".
- 🔴 **OPEN — outreach still unarmed.** `RESEND_API_KEY`, `TAG_POSTAL_ADDRESS`, `OUTREACH_TOKEN` are
  absent from the TAG-ai Vercel project; the drain has reported `skipped/unarmed` since 2026-08-20.
  ⚠️ **Never send outreach through a Resend connector directly** — it bypasses the queue's
  `UNIQUE(email)`, the watermark, the identity gate, opt-outs and the sent log. Set the three
  variables in Vercel; the drain sends 15/day through every rail.

- ✅ Live in production, all endpoints verified including end-to-end NLP.
- ✅ Public portal is now the **Texas ISD Financial Resource Guide**: white-minimalist McKinsey/SWD design, penny-of-the-dollar visual, 6 audience lenses, share cards, Methods & citation section, AI disclosure + TAG footer.
- ✅ `static/map.html` ("seating chart" of all 1,202 districts) works on phones — pan/pinch/tap; landmarks + named neighborhoods + plain axes.
- ✅ **"What the money buys" live on every district page** — student need, teacher turnover/experience/salary, STAAR, attendance, graduation, each vs structural peers + state; scored against what demographics predict; statewide lever chart (turnover 10.12% vs spending 0.01% — **cross-sectional, now labelled as description not leverage**; the within-district effects are ~25x smaller, see `scripts/build_economics_data.py`). Data: TEA Snapshot 2009–2024 joined to PEIMS (`scripts/ingest_tea_snapshot.py`, `scripts/build_outcomes_data.py`, `docs/WHAT_A_DOLLAR_BUYS.md`).
- ✅ Guided dollar (hover/tap → peer + state comparison + dollars at stake), zoom ladder, rebuilt landing page with the live statewide dollar, live-figures ticker under the header.
- ✅ **`/geomap` — the real map**: 1,005 Census TIGER boundaries joined to TEA numbers, coloured by turnover/spending/poverty/beats-prediction, neighbours outlined, "find my district" via in-browser point-in-polygon. No Mapbox, no API key, location never leaves the device. Covers ~92% of students (charters have no boundary).
- ✅ Three audit rounds + Monte Carlo audit complete (`AUDIT.md`, `docs/AUDIT_SCORECARD.md`); full suite green; CI enforces ruff + pytest + a JS parse check.
- 🔴 OPEN: user must rotate credentials pasted into chat (2026-07-22, 07-25, **2026-08-07**, and **2026-08-11 — the Vercel, Supabase and GitHub PATs were pasted a SECOND time and used to deploy**). All are live in Vercel env vars; rotating means updating them there too.
- ✅ **LLM is now DeepSeek** (`deepseek-v4-flash`), switched 2026-08-07 and verified live end to end: tool-calling loop completes, and answers match the database exactly (Dallas ISD 2024 = $23,420.00/student, spend and enrollment both exact). ~6s average. OpenAI ran out of credits, which prompted the move. Pricing changes by model, cache state, and time period; consult DeepSeek's current official pricing rather than preserving a historical comparison. `DEEPSEEK_API_KEY` present → DeepSeek, remove it → OpenAI. `/health` reports the live provider.
- 🌐 **The site is PUBLIC** (verified 2026-08-12: `/` returns 200, no noindex header — `SITE_PASSWORD` is unset). The gate middleware remains in the code; setting the env var + redeploy re-locks it.
- ✅ **First-party analytics live**: `site_visits` (daily counters — no IP/cookie/session, `/?d=…` counted as `/`) and `nlp_questions` (question text only). Read with `scripts/usage_report.py`. Vercel Web Analytics was rejected: its same-origin script never reaches Vercel here, and the CDN variant would need a third-party script in the CSP.
- ✅ **CLOSED 2026-07-31 and hardened 2026-08-22: `/query` authorization.** The original path ran as `postgres` and could read `auth.users`; `scripts/apply_nlp_role.py` applied the `nlp_reader` role and `NLP_DB_URL` is now mandatory. The role has schema `USAGE` plus SELECT on only two views, not CREATE/base-table/public-function rights. The later forensic found that lexical SELECT checks still admitted session/resource functions, so SQLGlot now rejects every unreviewed AST node, function, operator, and relation before execution. Prompt injection remains a resource/spend risk and is never described as harmless. See `docs/AUDIT_2026-07-31.md` C-1 and `docs/TECH_STACK_AND_AGENT_SKILLS.md`.
- ✅ **The site now survives a dead database.** 14 endpoints were always DB-free but the front door wasn't, so a paused free tier blanked everything. `/fallback-index` (built by `scripts/build_fallback_index.py`, 1,216 districts + dated statewide snapshot) backs the picker, search, hero figure and district heading; `renderAll()` renders each section in its own try/catch so one failure can't take the other fifteen. Verified headless with `SUPABASE_DB_URL` unset: hero, credential block, search and five district sections all render.
- ✅ Full repo audit 2026-07-31 (`docs/AUDIT_2026-07-31.md`): git history, secrets, deps, deploy configs, API, NLP path, all pages and payloads, every statistic, tests, CI, docs. All medium/low findings fixed. Verified clean: no live secret in 293 history blobs, no SQL injection across 22 call sites, every headline statistic re-derives, geolocation never leaves the device, vendor CRM never committed.
- ✅ **`/query` call ceiling now counts in the DB** (`sql/create_nlp_usage.sql`, atomic conditional-increment), so `QUERY_GLOBAL_LIMIT`/min and `QUERY_DAILY_LIMIT`/day hold across every serverless instance instead of per-process. The SQL was applied and later verified end to end. Missing metering degrades to a small per-instance trickle, not an unbounded fail-open path. Still set a provider-side spend limit or prepaid ceiling — these bound calls, not dollars.
- ✅ Payload sizes measured and dismissed: Vercel serves Brotli, so `/district-geo` is 220 KB on the wire and every per-district payload is 1–2 KB. The old “PR #2 pending” note is historical, not current merge guidance.
- ✅ **HISD takeover analysis** — `/takeover/houston` from `static/takeover_data.json`. Difference-in-differences vs 13 districts matched on PRE-takeover size+poverty. Houston **+5.5 vs +0.0**, rank 1 of 14, 1st of 30 large districts. Parallel pre-trends hold. **Special education students went backwards (−1.8 vs comparison)** — reported, not buried. `scripts/build_takeover_data.py`.
- ✅ **Equity layer** — `/district/{id}/equity` + `/equity/texas` from `static/equity_data.json` (TEA District STAAR, SY 2024 + 2025, 99.8% join). Headlines **how a district's low-income students do**, benchmarked against poor students statewide — **never the gap**, which correlates ~0 with how poor students actually do. Reported at **Meets**, not Approaches.
- ✅ **Bond story** — `/district/{id}/bonds` + `/bonds/texas` from `static/bond_data.json`. **Source corrected 2026-08-11**: this ran for weeks on a municipal-advisory vendor's Excel export credited to the *Texas Secretary of State*, with a note claiming no agency publishes school bond elections statewide. Both false — the **Texas Bond Review Board** publishes all of them on `data.texas.gov/d/kbmc-qmvg` and always did. Now ingested first-party by `scripts/ingest_bond_elections.py`: **4,992 decided propositions 1958–2026, 952 districts, 100% matched**, $327.6B asked. The vendor file was two years stale (404 propositions short, all recent) and carried spreadsheet `Subtotal:`/`Grand Total:` rows. Two published claims were corrected — **Wills Point ISD** (listed as never having a voter-approved bond; it carried $69.9M on 2025-11-04 after five straight defeats over 21 years) and **Louise ISD** ($9M, 2026-05-02). `scripts/district_match.py` resolves by name+county; `scripts/audit_bond_match.py` fails the build if a shared name resolves without the county agreeing. Carries the **bond→outcome test**: passed −0.27 vs defeated −1.58, **+1.31, CI +0.07/+2.55, p=0.038** (310 bonds, 153 districts) — **unchanged by the refresh**, because the new elections are too recent to have outcomes. Ships with a `fragile` flag; the page says *suggestive, not settled*. **Never ingest the two vendor companion CSVs — they carry a CRM (named reps, revenue, commissions). Going first-party removed the temptation.**
- ✅ **Debt outstanding — the stock, not the flow** (NEW 2026-08-11) — `/debt/texas` + `/district/{id}/debt` + the "What is still owed" section on `/forensics`, from `static/debt_data.json` (no DB). Every previous debt figure was PEIMS debt *service* — what was paid in a year. This is the balance: **Texas districts owe $236.7B as of fiscal 2025 — $148.4B principal and $88.3B of interest nobody has paid yet (37.3%)** — and on debt already sold it **clears in 2061**. **169 districts still carry capital appreciation bonds**, which pay nothing until maturity: **$2.36B of deferred interest against $440M of principal**. First-party from the Board's own issuer index and per-issuer series (`scripts/ingest_brb_debt.py` → `scripts/build_debt_data.py`), never the aggregator that led us to it. 967 issuers, 100% resolved to TEA numbers.
  ⚠️ **The repayment ratio is only ever taken at a district's PEAK year.** On an outstanding balance it climbs by itself as principal retires: Leander ISD reads 4.5x in 2014, 20.1x in 2025 and 396x in 2030 with no deal having changed. Also refused where the series has a **gap** (Ysleta ISD reports 2005–2012 then 2020+; its largest reported total postdates the 2015 4:1 cap, so no such deal could exist) or **predates the record** (La Joya opens at 90.5x). 63 districts get a ratio (1.22x–10.84x); 106 keep their deferred interest without one. `tests/test_debt.py` enforces all three refusals.
  ⚠️ **Dedupe by the Board's id, never by name.** Its index lists three issuers under two names each — including one id serving identical data as both "Highland Park ISD (Dallas)" and "Highland Park ISD [Amarillo]". Matching on name double-counts $406.5M.
  ⚠️ Rows after fiscal 2025 are the **amortisation schedule** for debt already sold — not history, not a forecast. Never sum them with history.
- ✅ **Campus layer — what the district rating hides** (NEW 2026-08-11) — `/campuses/texas` + `/district/{id}/campuses` + the "What the district rating hides" section on `/forensics`, from `static/campus_data.json` (no DB). Every other figure here stops at the district, which is where money is reported and the wrong unit for what a family asks. **138,664 students attend a campus Texas rates D or F inside a district Texas rates A or B** — 221 campuses, 73 districts, 21,415 of them at a campus rated **F**. And the spread is the rule: **779 of 890** districts with 2+ rated campuses contain campuses at different letter grades, 196 span three or more. First-party from TEA's Enhanced Statewide Summary workbook (`scripts/ingest_tea_accountability.py` → `scripts/build_campus_data.py`), 9,084 campuses.
  ⚠️ **Three refusals, all tested.** "Not Rated" is missing data, **not** failure — 525 campuses, all excluded (counting them would invent 525 failing schools). Alternative-education campuses are rated on a different scale and are excluded from the headline, with the including-them figure published beside it (only 2 of 223, so the finding does not turn on it). And **no campus is ranked against a campus in another district** — the claim is the gap INSIDE a district; a statewide campus league table is a far more dangerous artefact and `tests/test_campuses.py` fails the build if a campus number appears in any statewide list.
  ⚠️ The campus list is sorted **worst first** and unrated campuses sort past an A, so `rows[0]`/`rows[-1]` reads best/worst backwards — that shipped once (Houston ISD reported `worst=A`). Derive both from the grades.
- ✅ **Economics layer** (incl. **who pays**: local/state/federal from GROSS local collections — TEA reports M&O revenue net of recapture, so net understates property-funded districts; statewide 54/37/9) — `/district/{id}/economics` + `/economics/texas`, served from `static/economics_data.json` (no DB). What you pay (tax bill on a $300k home, what leaves under recapture), where it goes (teaching vs buildings vs debt — **debt sits OUTSIDE TEA's operating total, compose don't subtract**), what each lever actually bought (within-district first differences, 3-yr, clustered SEs), and who does better (matched persistent outperformers). `scripts/ingest_tea_property.py` + `scripts/build_economics_data.py`.
- ✅ **Forensic file** — `/forensics` page + `/forensics/texas` + `/district/{id}/forensics`, served from `static/forensic_data.json` (no DB). Composes four questions Texas publishes in four incompatible files: what sits **outside** TEA's operating total ($13.9B/yr in debt service statewide, median $1,538/student), **who pays** from GROSS local collections, what the **ballot** promised, and where results **landed** vs what need predicts. **Deliberately no combined score** and no per-district causal claim; every flag states a published number against a published threshold, and tests enforce both. `scripts/build_forensic_data.py`.
- ✅ **17-year trajectory** — `/trends/texas` + `/district/{id}/trends` + the "Seventeen years" section on `/forensics`, from `static/trend_data.json` (no DB). Turns the snapshot into a direction: six measures per district, fiscal 2009–2025, in constant 2024 dollars, each against the state's own line. The squeeze, statewide: **instruction's share of the operating dollar 57.8% → 54.5%** (−$369/student real) while **debt service $1,507 → $2,441** and **security $96 → $232 (2.4x)**; federal peaked $2,798 (2022) → $1,432; and in **2025 operating revenue fell below operating spending statewide for the first time in the window (+$3.0B 2022 → −$1.6B)**, with 44.4% of districts short, enrolling 67.7% of students. Every headline is re-derived on a **balanced panel** of the 1,142 districts reporting in both end years (−3.2 vs −3.3 pts), and tests assert each finding still matches its own series. `scripts/build_trend_data.py`.
  ⚠️ **Operating balance is operating revenue vs operating spending ONLY.** `all_funds_other_revenue` tracks debt service almost exactly year by year — it IS the I&S debt levy — so including it while excluding debt service understates deficits badly (it gives 12% for 2025 instead of 44%). Never use the `..._and_other_revenue_and_reca` column for an operating comparison.
- ✅ **Provenance chain closed** — `tests/test_provenance.py` (23 tests) re-derives EVERY published headline from the state's own file, longhand, without importing any builder: a wrong number can no longer validate itself. The 18 MB CSV is not committed, so `scripts/build_provenance_fixture.py` freezes the per-year statewide sums (26 KB, `tests/fixtures/provenance.json`) plus a **SHA-256 of the source**, and 17 of the 23 run with no CSV present — the guarantee holds in CI. `scripts/verify_artifacts.py` closes the last link by rebuilding every artefact and diffing byte for byte, which is the only check that catches a builder edited and never re-run, or an upstream restatement. Chain: TEA file → CSV (hashed) → fixture (committed) → artefact (diffed) → page (tested). **`tests/test_provenance_layers.py` (9) extends it to the layers that arrived later** — bonds, debt, campuses and the crosswalk — re-counting each headline from the raw CSV with the `csv` module, no builder imported. Its last test fails the build if a NEW artefact appears in `static/` with no re-derivation anywhere, so the gap cannot silently reopen.
- ✅ **`verify_sources.py` now checks ATTRIBUTION, not just liveness** (2026-08-11). It passed the wrong bond publisher every run, because the Secretary of State page returns 200 from a `sos.state.tx.us` host — it proved the link was *alive* and that the host matched the publisher we had *wrongly named*. Every source now declares `proves_it`: strings that must appear in what the URL actually serves. TEA's pages name their own product; the Census zip names its members in the archive header; `data.texas.gov` and `data.brb.texas.gov` declare an `attribution_url` pointing at their metadata/index API, which returns the publisher *as the State of Texas states it* — a stronger proof than our own claim. The old citation now fails the check. **A live link to the wrong file is worse than a dead one, because it looks checked.**
- ✅ **The reclassification caveat is RETIRED, with evidence** (`meta.reclassification_check`). Instruction's 57.8%→54.5% fall is real reallocation, not a recoding, four ways: the 16 function codes still sum to the reported operating total every year (0.9996–1.0000); risers +4.25 and fallers −4.25 cancel to a **zero residual**; **no single function absorbs it** (largest riser +1.07 vs instruction −3.26 — a recoding shows one bucket swallowing it); and it **predates COVID** (−1.75 by 2019) and **outlives the federal cliff** (−1.31 since 2022). Recomputed on every build so a future TEA release cannot quietly change the answer.
- ✅ **Window robustness published** (`meta.window_checks`): the finding holds on 17, 10 and 6-year windows (−3.27 / −2.32 / −2.43 pts; balanced panel 87.2% / 94.9% / 97.4%). A shorter window buys panel coverage and costs finding strength and makes nothing more accurate — so all three ship. **Agreement is the robustness result; disagreement would be the finding.**
- ✅ **Clickable lineage — why is this number this number** (NEW 2026-08-16) — `GET /district/{n}/lineage/{metric}` + the panel that opens when a reader clicks a per-student revenue figure on a district page, + the `district_lineage` MCP tool. Shows numerator, denominator, **which student count the denominator IS**, the formula, the year, the publisher, a link to the original file, and the verdict of a **publication gate** (`src/lineage.py`). VERIFIED requires four questions to pass — **computability, arithmetic, correctness, freshness, aim** — and each has a real failure on record behind it. `gate()` **cannot return VERIFIED when `recomputed_from` names the artefact being checked**, and a figure nobody independently recomputed is **UNVERIFIED, not VERIFIED**: a badge nobody earned is worse than no badge. Covers the four revenue figures for all 1,202 districts; **wave 2 (2026-08-17) added the spending card and the statewide headline**: the three real spending divisions (instruction/debt/operating per student, `allocation.lineage`, second road `scripts/recompute_spending.py`) plus `GET /lineage/texas/{metric}` for the front page's $109.4B (`meta.lineage_statewide`) — **a SUM gets arithmetic "n/a", never an invented denominator**, and the fixture carries `total_disbursements` so CI re-derives the headline from the SHA-anchored source sums. The card's composed total (rounded operating + rounded debt) and its "everything else" subtraction get NO division lineage — the federal_pct rule again. 8,416 figures now checked at build; disagreement refuses the write. Debt/outcome figures still publish result-only until their builders emit numerators.
  ⚠️ **The second road must stay a second road.** `scripts/recompute_revenue.py` re-derives all four figures with the standard-library `csv` module — no pandas, nothing shared with the builder — and **the build refuses to write `economics_data.json` if any of the 4,808 figures disagree**. `tests/test_lineage.py` parses its imports with `ast` (grep failed on the docstring) so it cannot become a wrapper around the builder. What agreement proves: the pandas pipeline — year selection, merges, index joins, the recapture add-back. What it does NOT prove: both roads read `prepare_data.py`'s cleaned CSV, not TEA's workbook, and name the same 60-char-truncated columns, so a wrong TEA→CSV mapping would be reproduced by both. That link is held by the SHA-256 in `tests/fixtures/provenance.json` and by `verify_sources.py`. **Never let the prose claim more than that** — the first draft said "an independent re-read of the publisher's own file" and had to be corrected in three places.
  ⚠️ **`.vercelignore` is a silent third environment.** It drops all of `scripts/`, and `src/sources.py` reads `scripts/freshness_vintages.json` at request time — so the deployed site answered "we do not know" for freshness and dropped **every** verdict to UNVERIFIED while tests stayed green, because the repo checkout has the file. Re-included with a test that fails the build if the re-include disappears. **Anything under `src/` that opens a file outside `src/`/`static/`/`api/` needs a re-include AND a test.**
  ⚠️ **Freshness is "as last recorded", never "the publisher's latest".** `fresh=True` is the absence of a recorded newer release; the daily watchdog asks the publishers and turns red but **never writes its answer back**. The panel says "Source current, as last recorded?" and states the record's date. `tea_staar_district` now carries `known_stale: true` so being a release behind is machine-readable instead of buried in a JSON comment.
  ⚠️ Only what VARIES is stored per district — the formula, unit and denominator name live once in `meta.lineage_templates`. Emitting them per district nearly doubled the artefact for no new fact, and 1,202 copies of "the formula" is 1,202 chances for one to differ. The three revenue **percentages deliberately get no lineage**: `federal_pct` is 100 minus the other two, a subtraction, not a division.
  ⚠️ `economics_data.json` is now in `verify_artifacts.py`'s rebuild-and-diff chain (it never was, despite being what the forensic and trend layers read), and the downstream builders are passed `--economics` from the temp build so drift cannot hide until a second run. `verify_live.py` covers the endpoint including the verdict itself: **26 checks, was 21**.
- ✅ **MCP server** — `POST /mcp` speaks **Model Context Protocol 2026-07-28** (`src/mcp_protocol.py` wire format + `src/mcp_tools.py` tools, `docs/MCP.md`). **Eleven** read-only tools over committed JSON: `find_district`, `district_money`, **`district_lineage`**, `district_forensics`, `district_trends`, `district_bonds`, `district_debt`, `district_campuses`, **`district_national`**, `texas_overview`, `compare_districts`. **Every result carries the payload's `limits`**, so caveats travel into someone else's chat attached to the number. **MRTR (SEP-2322) is wired to the name-collision problem**: a tool given an ambiguous district name returns `resultType: "input_required"` listing the real candidates with enrolment instead of guessing — thirteen Texas names belong to two districts each, and guessing is how bond history reached the wrong district once already. No `requestState` is minted (arguments are re-sent), so it stays stateless. **The connect-time `instructions` now read their figures from the artefact** — they claimed "4,588 bond elections" for as long as the bond layer was stale, and kept claiming it after the refresh. Hand-written, no SDK dependency — a bad dep fails every Vercel build, and the needed surface is three methods. **`/query` is deliberately NOT exposed** (prompt-injection history, spends DeepSeek tokens); every tool is a deterministic read with no DB, so it survives a paused Supabase. 2026-07-28 is what made this possible at all: it removed the `initialize` handshake and `Mcp-Session-Id`, so a stateless serverless app behind a round-robin no longer needs session affinity.
- ✅ **Historical deployment checkpoint (2026-07-26):** security headers, `/query` threadpool+timeout+global cap, table twins, property/tax/recapture ingest, and the economics layer were live then. Do not extend that statement to later repository changes without matching deployment proof.
- 🔴 OPEN: a fifth credential was pasted into chat 2026-08-11 (`od_live_…`, tryopendata.ai). Rotate with the other four. It was used read-only for discovery and never committed.
- ✅ **DEPLOYED 2026-08-11 and verified**: `verify_live.py` 21/21 green, `vercel.json` unmodified by the CLI, Wills Point ISD + Louise ISD off the false-claim list (93 flagged, was 102), `/debt/*` + `/campuses/*` live, MCP serving 9 tools. `sql/create_nlp_usage.sql` and `sql/create_cron_runs.sql` APPLIED — `/query` incremented `nlp_usage` end to end, so the global ceiling is real; `/api/cron/runs` reports `available:true`.
- ✅ **2026-08-12 wave (all live, verify_live green).** One **masthead** on all 10 pages (7 tabs + More menu; zero orphan pages — link-graph audited 9/9 reach from everywhere; masthead links carry `?d=` so the district follows the reader). **AI disclosure**: `/transparency` (legal text + making-of), a hero strip on the report, a one-line footer on every page and both halves of every outreach email — all test-enforced. **Frame-honesty extended**: `?` explainer on every card (18 plain-English entries, first sentence IS the answer), wow engine (one number per persona per lens), did-you-know rotator, split-canvas heroes with the assembling penny dollar (district-personalised; statewide render never clobbers it), district boundary draws itself as a signature (from /district-geo).
- ✅ **The 10x edge function**: `/` injects per-district head server-side from committed artefacts (DB-free) — district name in `<title>`, own canonical, OG/Twitter tags, `static/share/{num}.png` (1,216 Pillow cards, committed; `/share/{num}.png` route with default fallback), Dataset+GovernmentOrganization JSON-LD. Card/meta lead with the OPERATING figure only — the all-funds total lives only in the DB view and must never be reproduced from artefacts (it disagrees: econ total_per_student excludes construction). `tests/test_og.py` locks it.
- ✅ **Outreach history and durable floor** — the first three local-sender waves reached 571 districts; the tracked 2026-08-17 wave brought the committed total to **671 unique addresses**, exactly matching `data/outreach_watermark.json`. The server-side runner in the top status item is now the current path. Do not follow the superseded 571-row instruction preserved in older log entries: both local and server-side paths must refuse whenever durable sent state resolves below the committed 671-address floor, and send-time opt-out/suppression checks remain fail-closed. Never infer that the remaining queue is ready from the historical count; verify the private server status and durable database state before any operation.
- ✅ **Outreach KPI snapshot** (`scripts/outreach_kpi.py` + `.github/workflows/outreach-kpi.yml`, Mondays 12:30 UTC; a manual missing-secret run is a readiness probe, while a scheduled missing-secret run warns, records `NOT CONFIGURED`, and fails until `RESEND_API_KEY` + `SUPABASE_PAT` are repository secrets). Pulls each message's last event from Resend into `data/outreach_kpi_report.csv` and upserts to `outreach_status` so engagement outlives Resend's retention. **First reading, 2026-08-13 13:40 (271 of 571 checked): 74 opened, 186 delivered-unopened, 6 bounced, 5 suppressed, 0 opt-outs.** Two findings were instrument failures, not behaviour: the 08-11 batch of 71 shows ZERO opens against 40.3% the next day (p≈6e-16 — open tracking was off, that send predates the `gus@ubntag.com` default added 08-12 04:38 and the tests around it went from `reports@txisd.dev`); and **0 clicks across all 571 is a disabled toggle, not a verdict** (0 of 58 openers, p≈1/450). ⚠️ The report CSV is gitignored and container-only unless the run had a PAT. Campaign-level click-through IS already counted first-party as `src:email` in `site_visits` since 08-12 12:50 and has never been read.
- ✅ **Per-recipient journey tracking** (NEW 2026-08-13) — `sql/create_visitor_tracking.sql` + `src/tracking.py` + `static/track.js` + `scripts/journey_report.py`. Opens (own pixel `/px/{rid}.gif`, not Resend's), clicks, every page, visible dwell, and return visits, per named recipient. **The entry condition is the whole design**: tracking begins ONLY when someone arrives with a `?rid=` we minted and mailed; an httpOnly cookie carries it forward so a return visit weeks later still attributes. Anonymous visitors keep the old guarantee untouched — daily counters, no cookie, no row. `outreach_recipient` is the only place a rid becomes a name; drop it and the behaviour table is anonymous. Wave 1's 571 sends are **untracked and cannot be retrofitted** (their links are already delivered); `--campaign` labels each wave so one can be reported or deleted alone.
  ⚠️ **The disclosure ships with the feature or the site is lying.** The old blanket promise ("We measure the site, never the visitor: no cookies, no IP addresses, no visitor IDs") was FALSE the moment this landed. `static/index.html` + `static/about.html#privacy` now state the exception plainly, the tracked email says so in both halves, and `tests/test_tracking.py` fails the build if any of that text disappears — including a test asserting the old sentence never returns verbatim.
  ⚠️ **The schema and privacy controls apply themselves** (`src/migrations.py`, called from the lifespan): every startup checks the tracking, intelligence, and outreach sentinels, the journey-view definition marker, and the cron-log privacy marker. Missing application-owned objects are created; stale views, constraints, and access controls are refreshed. **`CREATE TABLE IF NOT EXISTS` is NOT race-safe** — 6 concurrent cold starts on an empty DB produced 1 success and 5 `pg_type` duplicate-key errors, so a **transaction-scoped** advisory lock serialises them (xact-scoped, not session: Supabase's pooler is transaction-mode and a session lock can outlive its connection). Verified: 8 racing workers → 0 errors, 2 tables, anon/authenticated/nlp_reader all denied. The under-lock re-check does NOT reliably observe a peer's commit, so nothing depends on it — correctness is lock + idempotence. The unattended DDL never drops tables, views, or columns and never mutates finance data. The bounded data-changing exception is `CRON_LOG_PRIVACY_DDL`, which clears unsafe legacy free-text cron details, constrains future values, and reapplies restricted access; `tracking_schema=ready` means startup observed the current sentinels and markers, not that grants are continuously audited.
  ⚠️ `SUPABASE_PAT` must be set during the send, or journey tokens never reach the database and those clicks cannot resolve to a person. Recoverable the same day with `scripts/sync_outreach_state.py --push` (pushes `data/outreach_recipients.csv`), but only for recipients who have not clicked yet.
  ⚠️ The open pixel must never 500 — it renders inside a superintendent's inbox as a broken image. Every tracking path reads the pool through `_pool_or_none()`; reading `app.state.db_pool` directly raised `AttributeError` and was caught in testing.
- ✅ **DEPLOYED LIVE 2026-08-14** — PR #11 + #12 merged to master; Vercel's Git integration builds production on merge (direct push to master is **branch-protected and rejected**, so a PR is the only path — that is how #7–#12 all shipped). Verified against txisd.dev: `verify_live.py` **21/21**, `/px/{rid}.gif` returns a real 43-byte GIF89a, `/static/track.js` 200, **an anonymous visitor receives no `set-cookie`**, the new disclosure is live on `/about`, and the old false sentence returns **count=0**. `/health` now also reports `tracking_schema` so a failed migration can never again look identical to a working one from outside.
- ✅ **The map drew five districts on the wrong land, and now cannot** (2026-08-14) — `build_district_geo.py` keyed a dict by normalised name, so of the **11 Texas district names that belong to two districts each**, both twins resolved to whichever TEA number the finance file mentioned last: one got the OTHER's territory, one vanished. LIVE and wrong on TIGER2024: Highland Park ISD (Potter) drawn as **Dallas's**, Northside ISD (Wilbarger) drawn as **San Antonio's**, plus Chapel Hill, Valley View, Hubbard. TIGER2025 ships a polygon for the second twin of all 11 pairs, so the naive join overwrote 11 times and still printed **`unmatched polygons: 0`** — which reads as an improvement on 2024's 1. Fixed by resolving a shared name via **the county the polygon physically sits in** (the same name+county key the bond layer uses; a TEA number's first 3 digits ARE its county code), refusing rather than guessing, and a **hard accounting assert** — polygons in must equal written + unmatched + ambiguous + dropped or the build dies. The TIGER vintage is now read off the filename, never hardcoded. Now **1,016 districts, 0 unmatched, all 22 twins verified by point-in-county**; crosswalk rebuilt (exactly 11 rows, `has_boundary` False→True). `tests/test_geo_boundaries.py` fails against the old payload and passes against the new. ⚠️ Rebuilding needs `--counties` (a TIGER county shapefile) whenever any name is shared; without it the build REFUSES rather than guessing.
- ✅ **`/query` degrades instead of failing open, and is verified live** (2026-08-14) — the shared ceiling failed OPEN whenever metering was unreachable, which left the bill unbounded during exactly the outage nobody watches (a free-tier pause takes `nlp_reader` down too). Fully closed is also wrong: metering can fail while the DB is up. So it **degrades** to `QUERY_DEGRADED_LIMIT` (5/min/instance); a missing `nlp_usage` table still fails open, because that is a migration, not an outage. **This bounds CALLS, not DOLLARS — only a provider-side cap does that, and it is still owed.** `verify_live.py --with-query` now asks the live agent the one question with a checkable answer and **fails on "100"** specifically (the LIMIT-on-a-COUNT regression already shipped once); the daily monitor runs it. Verified live: still answers 1,310.
- ✅ **A committed watermark stops a re-send** (2026-08-14; now 671) — `data/outreach_sent.csv` is gitignored and `_remote_emails()` returns an EMPTY set (not an error) when `SUPABASE_PAT` is unset, so a fresh clone can resolve a skip-list of zero and would otherwise re-email prior recipients. `data/outreach_watermark.json` is committed and is the floor; `skiplist_shrank()` refuses the send when the resolved list is smaller, because a skip-list can only grow. Its total must equal the waves it lists.
- ✅ **Freshness checks now prove they watch the right PRODUCT** (2026-08-14) — audited all 10. `tea_recapture` was a **false alarm** (watched the excess-local-revenue landing page; that list is a product `ingest_tea_property.py` never reads — now watches the recapture workbook by Last-Modified). `tea_staar_district` is genuinely stale but was right **by coincidence**: its pattern matches the statewide "All Results Analysis" PDF, not the district file. Each `page_year` source now carries a `product_proof` string that must appear beside the matched year (like `verify_sources`' `proves_it`); year-found-but-product-absent returns UNVERIFIABLE instead of a confident OK/NEWER. STAAR deliberately has none — inventing one would launder a coincidence into a guarantee — and a test fails the build if a source lacks one without saying why.
- ✅ **Bond refresh 2026-08-14**: the Board **backfilled vote tallies** for Crowell ISD (105–82, 2021) and Como-Pickton CISD (367–215, 2023) — a real correction, so the check was right to fire. 4,992 propositions unchanged; `votes_reported` 80.0→80.1%; `verify_artifacts` caught `forensic_data.json` as downstream drift and it was rebuilt.
- ✅ **The equity headline is dated** (2026-08-14) — it led with a percentage and no year, which reads as "now". It is a release behind (TEA published SY2025-26 on 2026-06-16 and put the file behind a login), and being a release behind is defensible while being a release behind and undated is not. `schoolYear()` renders 2024–25, not the ambiguous "SY 2025".
- ⚠️ **STAAR SY2026 needs a HUMAN download**: TEA's own STAAR Aggregate Data page now returns 403 ("Login required") and the district file lives only in the Cambium Research Portal (txresearchportal.com), whose bulk CSV export is gated behind email verification (`/Query/Offline` + `/Query/Verify`). Client id for Texas is `c21a285e-d0fa-4c1a-9070-32b0d50178e1`. Save to `data/staar_district_2026.csv`, then `ingest_staar_district.py --files …2024 …2025 …2026` then `build_equity_data.py`.
- ✅ **Outreach map — where the campaign landed** (NEW 2026-08-14) — `/ops/outreach` + `/ops/outreach-data` (`src/outreach_map.py`, `static/opsmap.html`). One dot per district in the mailing list on the real TIGER2025 boundaries, coloured by funnel position; hover gives sent/opened/clicked dates + pages + dwell, double-click opens that district's report. **Mapbox was rejected**: `/geomap` already renders 1,016 boundaries with no API key, so a vendor basemap would add a token, a CSP entry and a build dependency to draw a worse version of a map we own.
  ⚠️ **SIX states, not four.** `unknown` (sent but never checked — 300 of 571; painting them "not opened" asserts a measurement nobody made) and `bounced` (never arrived ≠ ignoring us) are separate on purpose. **`clicked` reads ZERO for wave 1 and always will** — click tracking was a disabled toggle during those sends and the links are delivered; `meta.limits` says so in the payload.
  ⚠️ **Not public, and not by convention.** Every dot is a named superintendent's behaviour. `OPS_TOKEN` (constant-time compare), **404 not 403** when wrong/unset, noindex + no-store, linked from nowhere, and deliberately NOT `SITE_PASSWORD` (which would take the whole portal private). A test asserts no email address can reach the payload. 291 districts with no contact are counted, never drawn — "not sent yet" would read as a choice, not a gap.
  ⚠️ Private pages are excluded from the public design-system tests **by name** (`PRIVATE_PAGES` in `tests/test_static_pages.py`) but STILL get the JS parse check via `ALL_PAGES` — a syntax error breaks a private page just as badly.
- ✅ **Watchdogs**: `.github/workflows/monitor.yml` (daily, no secrets: /health healthy+connected, cron-runs gaps, verify_live, check_freshness). `scripts/check_freshness.py` + `scripts/freshness_vintages.json` — bumping the vintage file is how a release is acknowledged. **Of the 3 layers it first flagged, 1 was real and 2 were the check watching the wrong artefact** (resolved 2026-08-14). TIGER2025 ingested — and doing so exposed that 5 districts were LIVE with another district's territory (see the geomap bullet). `tea_recapture` was a FALSE ALARM: it watched the excess-local-revenue landing page and fired on its '2026-2027' school-year label, but that is the ELR district LIST, a product `ingest_tea_property.py` never reads — it now watches the recapture-paid workbook we actually ingest, by Last-Modified (no 1994-2027 file exists). `tea_staar_district` is GENUINELY stale but needs a HUMAN: TEA put its own STAAR Aggregate Data page behind a login (403) and the district file now lives only in the Cambium Research Portal, whose bulk CSV export is email-verification gated. Its `page_year` pattern also matches a statewide PDF, not the district product — right about 2026 by coincidence, not evidence. **A freshness check that watches a neighbouring page instead of the file you ingest cries wolf, and a check that cries wolf gets ignored.** Still open: bond_elections updated 2026-08-14 04:02 UTC (upstream moved during that session).
- ✅ **Distribution**: `/feed.xml` Atom (DB-free, content-hash ids), `.vercel.app` → txisd.dev 308 redirect (`redirects` block is SAFE; only `rewrites` is banned), `/.well-known/mcp.json` discovery (scanner whitelists exactly that path). Raw-data archive: `scripts/archive_raw_data.py` + full SHA-256 manifest of vintage 2026-08-12 committed in `docs/RAW_DATA_ARCHIVE.md`; **publishing the 13MB tarball as a GitHub release is blocked from this sandbox (org policy) — run the script from any normal machine**. Outreach CSVs deliberately excluded from the archive (private contact data).
- 🎁 **TAG ai assets**: `district-dossier.vercel.app` (Michelle Sanchez client tool, CB Global Luxury brand, 18 districts, no MLS data); TAG pipeline graphic at `/static/tag-pipeline.png` (hot-linked by outreach emails).
- 🔴 **OPEN (owner credentials):** rotate every credential previously pasted
  into chat and update the correct durable store. The TAG-ai Vercel project is
  Pro, but the live outreach drain is still unarmed; configure the rotated
  Resend key, postal address, and a new outreach token there. Keep Vercel Git
  integration as the single deployment path; Actions verifies the live tree
  but holds no Vercel deploy credentials.
  Also set a provider-side DeepSeek ceiling, publish the raw-data release, and
  ingest or acknowledge the gated stale source layers above.
- ⏭️ NEXT: write the HISD finding up for a reporter; then the **TAPR campus file** (teacher certification, and the 77% invisible above campus level). Older plan: bake outcomes in as the **spine** (every money fact ends in what it bought, with an error bar) — first the bond→outcome test on the district page; then the **HISD board-of-managers analysis** (did the 2023 takeover change results vs matched districts?); then the **TAPR campus file** for teacher certification.
- 📌 Measured within-district **at the Meets bar**: teacher pay +$5,000 → **+0.86** (CI +0.48/+1.23, the largest identifiable lever); class size −2 → +0.37; turnover −10 → +0.30 (**CI crosses zero**); spending +$2,000 → +0.15 (**crosses zero**); a passed building bond → −0.57 (crosses zero). The large thing is a persistent district effect, **77% unexplained**.
- ✅ **Silent failure is now visible** (2026-08-11) — `sql/create_cron_runs.sql` + `/api/cron/runs`. A job that never fires and a job that fires and writes nothing look identical from outside, which is how the intel cron failed silently for four days. Every firing records status (`ok`/`skipped`/`error`), duration and `rows_written`; `gap_days` and `wrote_nothing` are what you read. **The failure path had no record at all before this** — an exception 500'd and left nothing. The SQL was applied and the public aggregate endpoint was verified live; the 2026-08-22 hardening explicitly monitors both configured cron jobs.
- ✅ **One formatter, not three** (2026-08-11) — `src/format.py`. `_usd` existed in both `src/mcp_tools.py` (exact) and `scripts/isd_intel.py` (abbreviated) — **same name, opposite precision**: the briefing rendered $1.5M as **"$2M"** and $1,234 as "$1K", and still carried the `$-354` sign bug fixed in mcp_tools months earlier. Three name formatters (`_title`, `nice_name`, `_district_name`) each independently produced `D'hanis ISD` and `S And S CISD`. `tests/test_format.py` locks every one of those defects.
- 🟡 WATCH: Supabase free tier pauses after ~7 days idle → portal shows
  "database not connected" until woken in dashboard. Vercel is already on the
  TAG-ai Pro team; do not confuse it with the separate GOAT-UIX Hobby team.

## Key docs

`AUDIT.md` (all findings) · `DEPLOYMENT.md` (launch runbook) ·
`PROJECT_MAP.md` (visual map) · `docs/ENGINEERING_LOG.md` (session memory).
