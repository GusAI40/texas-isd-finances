# Core dependencies
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1        # TEA published one year of property values as legacy .xls
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0

# NLP and AI (LangChain 1.x — uses the supported create_agent API;
# langchain-community was sunset June 2026 but still provides the SQL toolkit)
langchain>=1.0.0,<2.0.0
langchain-community>=0.4.0
langchain-openai>=1.0.0
openai>=2.0.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Visualization (optional)
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

pyshp>=2.3.0  # Census TIGER shapefile reader (offline scripts only)
